development:
  adapter: postgresql
  host: localhost
  username: postgres
  password: postgres
  database: rails_sample_dev

  #adapter: sqlite3
  #database: db/development.sqlite3
  #pool: 5
  #timeout: 5000

# Warning: The database defined as "test" will be erased and
# re-generated from your development database when you run "rake".
# Do not set this db to the same as development or production.
test:
  adapter: postgresql
  host: localhost
  username: postgres
  password: postgres
  database: rails_sample_test

production:
  adapter: postgresql
  host: localhost
  username: postgres
  password: postgres
  database: rails_sample_prod
